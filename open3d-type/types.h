#pragma once

#include <cassert>
#include <cmath>

#include <iostream>

namespace open3d
{
    namespace DIM
    {
        typedef enum _Type
        {
            X = 0,
            Y = 1,
            Z = 2,
            W = 3
        } Type;
    };
}
